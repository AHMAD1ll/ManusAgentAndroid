name: Build Manus Agent APK (Absolute Solution)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1. تنزيل الكود الخاص بك
    - name: Checkout code
      uses: actions/checkout@v4

    # 2. إعداد بيئة Java
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    # 3. تثبيت إصدار Gradle الصحيح والمحدد
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3
      with:
        # *** الإصلاح الحاسم والنهائي هنا ***
        # نحن نجبر GitHub Actions على استخدام الإصدار 8.6، متجاهلين أي إعدادات أخرى.
        gradle-version: '8.6'

    # 4. بناء التطبيق مع التنظيف
    - name: Build with Gradle
      env:
        MY_SIGNING_KEY_BASE64: ${{ secrets.MY_SIGNING_KEY_BASE64 }}
        MY_SIGNING_KEY_ALIAS: ${{ secrets.MY_SIGNING_KEY_ALIAS }}
        MY_SIGNING_KEY_PASSWORD: ${{ secrets.MY_SIGNING_KEY_PASSWORD }}
      run: gradle clean assembleDebug

    # 5. رفع ملف APK
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: manus-agent-apk
        path: app/build/outputs/apk/debug/app-debug.apk
